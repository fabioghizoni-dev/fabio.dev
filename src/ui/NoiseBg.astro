<div class="absolute inset-0 bg-gradient-to-t from-red-500 to-purple-500 -z-1">
  <svg width="100%" height="100%">
    <filter id="noise" aria-label="Filter Noise Gradient">
      <feTurbulence seed="2" numOctaves="2" type="fractalNoise" stitchTiles="stitch" baseFrequency="0.4"></feTurbulence>
    </filter>
    <rect width="100%" height="100%" filter="url(#noise)" style="mix-blend-mode: soft-light;"></rect>
  </svg>
</div>
